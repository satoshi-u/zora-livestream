{
  "name": "zora-livestream",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zora-livestream",
    "create-local": "graph create --node http://localhost:8020/ zora-livestream",
    "remove-local": "graph remove --node http://localhost:8020/ zora-livestream",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zora-livestream"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.24.0",
    "@graphprotocol/graph-ts": "0.24.0"
  }
}
